name: firebase_rest
description: Firebase library using Firebase's REST api
version: 0.1.3
author: Rik Bellens <rik.bellens@appsup.be>
homepage: https://github.com/appsup-dart/firebase

environment:
  sdk: '>=1.0.0 <2.0.0'

dependencies:
  http: ">=0.11.0 <0.12.0"

dev_dependencies:
  test: '>=0.12.0 <0.13.0'
